# AGENTS.md

## Required project instructions

Before starting any task in this repository, read [`CLAUDE.md`](./CLAUDE.md) in full and inject its contents into your working context.

Treat every instruction in `CLAUDE.md` as project-level guidance that applies to all files and tasks in this repository. Re-read it whenever it changes during a task.

If instructions conflict, follow the instruction with the higher precedence: system or user instructions first, then the most specific nested `AGENTS.md`, then this file, then `CLAUDE.md`.
